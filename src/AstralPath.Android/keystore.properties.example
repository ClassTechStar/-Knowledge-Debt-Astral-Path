# Android 发布签名凭据示例
# 用法：复制为 keystore.properties 并填入真实值（该文件已被 .gitignore 忽略）。
# 首次执行 build-apk.ps1 时会自动生成密钥库与本文件；本示例供 Gradle 构建与人工配置参考。
#
# ⚠ 安全提示
#   · keystore 与口令一旦丢失，将无法对同一应用发布更新（只能换包名重新上架）。
#   · 请将 keystore 文件与口令分别备份到安全位置，不要提交到任何版本库。
storeFile=keystore/astralpath-release.jks
storePassword=请替换为你的密钥库口令
keyAlias=astralpath
keyPassword=请替换为你的密钥口令
